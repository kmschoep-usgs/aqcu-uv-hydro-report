server:
  port: ${serverPort:8443}
  use-forward-headers: true
  ssl:
    key-store: ${keystoreLocation}
    key-store-password: ${keystorePassword}
    key-password: ${keystorePassword}
    key-alias: ${keystoreSSLKey:tomcat}

aqcu:
  reports:
    webservice: ${aqcuReportsWebserviceUrl}

javaToR:
  service:
    endpoint: ${javaToRServiceEndpoint}

aquarius:
  service:
    endpoint: ${aquariusServiceEndpoint}
    user: ${aquariusServiceUser}
    password: ${aquariusServicePassword}
    retries:
      unauthorized: ${aquariusUnauthorizedRetries:3}

security:
  require-ssl: true
  basic:
    enabled: false
  oauth2:
    resource:
      filter-order: 3
      id: ${oauthResourceId}
      jwt:
        keyUri: ${oauthResourceTokenKeyUri}

management:
  security:
    enabled: false

eureka:
  client:
    enabled: false

feign:
  hystrix:
    enabled: true

hystrix:
  shareSecurityContext: true
  command:
    default:
      execution:
        isolation:
          strategy: THREAD
          thread:
            timeoutInMilliseconds: ${hystrixThreadTimeout}
  threadpool:
    default:
      maxQueueSize: ${hystrixMaxQueueSize:800}
      queueSizeRejectionThreshold: ${hystrixMaxQueueSize:800}
      coreSize: ${hystrixThreadPoolSize:5}

asyncRetrieval:
  corePoolSize: ${asyncRetrievalCorePoolSize:10}
  maxPoolSize: ${asyncRetrievalMaxPoolSize:-1}
  queueCapacity: ${asyncRetrievalQueueCapacity:-1}

spring:
  profiles:
    active: default, swagger
  application:
    name: aqcu-tss-report
  jmx:
    default-domain: aqcu-tss-report

logging:
  level:
    gov.usgs.aqcu: INFO